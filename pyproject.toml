[project]
name = "assignment2"
version = "0.1.0"
description = "PRML Assignment 2 — Causal Language Model on PTB"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0",
    "numpy",
]

[tool.ruff]
target-version = "py312"
line-length = 120
